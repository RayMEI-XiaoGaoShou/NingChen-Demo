# 《佞臣》机制验证报告

验证时间：2026-04-17  
验证范围：当前工作版本 `C:\Users\happyelements\Desktop\佞臣 Demo\NingChenv4\NingChen-Demo-codex-difficulty-balance-inline-sync-36e3165\佞臣V3`

## 验证方法

- 逐项阅读实现代码，重点核对 `RoundStart`、`SchemePanel`、`SchemeFeedback`、`gameStore`、`fengDaozhiAdvisor`、`npcPromptContext`、`roundSettlement`、`CourtView` 等模块。
- 结合已有单测判断哪些链路已有自动化覆盖，哪些结论主要来自代码静态核查。
- 额外跑了以下相关测试：
  - `npm.cmd test -- src\game\roundIntelEngine.test.ts src\game\externalLineProgress.test.ts src\game\borrowedBladeEngine.test.ts src\game\npcPromptContext.test.ts src\game\fengDaozhiAdvisor.test.ts src\stores\gameStore.test.ts src\ai\prompts.test.ts`
  - 结果：`7 passed, 62 passed`

---

## 1）验证冯道之锦囊中对于地方军头的提醒是否是动态的

### 结论

结论：**是，已动态化**。  
地方军头对应的冯道之锦囊不是写死文本，而是根据该军头当前的 **信任度、对朝廷忠诚度、已解锁暗线数量、当前是否进入 watchful 状态、回合是否开放外部动作窗口** 动态生成。

### 证据

- 首页地方军头锦囊来自 `src/components/RoundStart/RoundStart.tsx:64` 的 `buildDominantExternalStageHint(...)`。
- `buildDominantExternalStageHint` 在 `src/game/externalActionHint.ts:39` 开始实现，会对所有存活的外部军头重新计算分数与阶段。
- 该分数直接使用：
  - `npc.trust`
  - `Math.max(0, 45 - npc.loyaltyToCourt)`
  - `unlockedSecrets * 10`
  - `npc.externalStatus === 'watchful' ? 8 : 0`
- 实际阶段文案来自 `src/game/externalLineProgress.ts:68` 的 `buildExternalLineProgress(...)`，其中会根据：
  - 信任差值 `trustGap`
  - 忠诚差值 `loyaltyGap`
  - 暗线差值 `secretsGap`
  - 是否开放外部动作窗口 `externalActionEnabled`
  动态切换“养信 / 探暗线 / 离心 / 等窗口”等提示。

### 额外观察

- 朝堂页地方军头卡片下方的“还差 xx 点信任 / 暗线已明 x/x / 当前正可试割据或造反”也是同一套 `buildExternalLineProgress(...)` 计算出来的，因此首页锦囊与朝堂页底层逻辑一致。
- 相关测试覆盖：
  - `src/game/externalLineProgress.test.ts`
  - `src/game/roundIntelEngine.test.ts`

---

## 2）验证施计页谶纬计谋的冯道之代拟是否有落地

### 结论

结论：**已落地**。  
谶纬不是只改了 prompt 文案，前端、store、prompt、预览组件、测试都已经串起来了。

### 证据

- 施计页点击“请他代拟”后，入口在 `src/components/SchemePanel/SchemePanel.tsx:307` 的 `handleFengDaozhiDraft`。
- 它会调用 `src/stores/gameStore.ts:609` 的 `requestFengDaozhiDraft(...)`。
- store 层会构造上下文 `buildFengDaozhiDraftContext(...)`，然后调用 `chatCompletionJson(buildFengDaozhiDraftPrompt(...))`。
- 谶纬专门要求双段输出：
  - `primaryText` = 谶辞/征兆
  - `secondaryText` = 解释/指向
  - 见 `src/ai/prompts.ts:577-596`
- 前端收到结果后，在 `src/components/SchemePanel/SchemePanel.tsx:328-330`：
  - `setOmenText(draft.primaryText)`
  - `setInterpretationText(draft.secondaryText ?? '')`
  - 并将两段合并写入施计文本
- 冯道之代拟面板本身也支持谶纬双段展示，见 `src/components/SchemePanel/FengDaozhiAssistPanel.tsx:51-52`。

### 测试覆盖

- `src/stores/gameStore.test.ts:382-395` 明确验证了谶纬代拟会返回双段文本。
- `src/ai/prompts.test.ts:334-336` 明确验证 prompt 中存在谶纬双段格式要求。
- `src/components/SchemePanel/FengDaozhiAssistPanel.test.tsx` 也覆盖了双段展示。

---

## 3）检查施计页冯道之军师（代拟）的 system prompt 是否是动态的

### 结论

结论：**部分动态，且已经吃进相当多状态，但没有完整吃进战役结果分支**。

### 已动态的部分

冯道之代拟上下文来自 `src/game/fengDaozhiAdvisor.ts:30` 的 `buildFengDaozhiDraftContext(...)`，其中会动态读取：

- 当前回合事件名称与摘要  
  - `ROUND_EVENTS[request.round - 1]`
- 目标角色公开身份与公开人设
- 该角色当前已解锁暗线数量  
  - `npc.secretThreads.slice(0, unlockedSecrets)`
- 上回往来
- 近两回合关系温度
- 近来得失
- 派系压力
- 玩家当前危险等级 `playerDangerStage`

这些动态字段最后都会被注入 `src/ai/prompts.ts:606-617` 的用户 prompt 中。

### 动态性的来源

其中“上回往来 / 近两回合关系温度 / 近来得失 / 派系压力”本身又来自 `src/game/npcPromptContext.ts:31` 的 `buildNpcPromptDynamicContext(...)`，所以冯道之代拟不是静态模板，而是吃进了：

- 最近 1 回合对该 NPC 的实际施计记录
- 最近 2 回合对该 NPC 的施计风格与成败
- 当前派系强弱变化
- 近期 backlash 余波
- 该 NPC 当前忠诚度、外部状态等

### 没有完整动态吃进去的部分

施计页右侧角色卡的“本回合公开表态”是动态的，见：

- `src/components/SchemePanel/SchemePanel.tsx:266`
- `src/components/SchemePanel/SchemePanel.tsx:721-722`

它调用的是 `getNpcRoundReaction(...)`，并且会显式带入：

- `shuCampaignState`
- `huainanCampaignState`

也就是说，**施计页展示层** 已经会随蜀地/淮南战役结果分支变化。

但冯道之代拟上下文 `buildFengDaozhiDraftContext(...)` 取的是 `ROUND_EVENTS` 的静态回合事件说明，并没有把 `shuCampaign.resolvedState` / `huainanCampaign.resolvedState` 直接注入代拟 prompt。  
因此当前状态可以概括为：

- **已动态**：暗线解锁、回合剧情、近两回合互动、派系压力、近期得失、玩家危险
- **未完整动态**：战役结果走向尚未直接进入冯道之代拟 prompt

### 建议

如果目标是让冯道之代拟也真正“看见”战役分支，建议后续把 `getRoundCampaignEventContext(...)` 或战役 resolvedState 摘要接进 `buildFengDaozhiDraftContext(...)`。

---

## 4）检查所有北周可被施计对象 NPC 的短期、中期、长期记忆机制是如何实现的

### 结论

结论：**当前是结构化记忆，不是 LLM 自己保留一份长期自由对话记忆。**  
NPC 的“记得你之前做过什么”，不是靠模型自己记忆，而是每回合重新从结构化状态中拼出 prompt context。

### 记忆分层

#### 短期记忆

来源：`src/game/npcPromptContext.ts:47` 的 `describePreviousDealings(...)`

- 只看最近 1 回合 `roundHistory[roundHistory.length - 1]`
- 如果上一回合你明确对这个 NPC 施计，就会写成“上回你曾以某计试他，成功/失败”
- 如果上一回合只是把他卷进重点人物链，也会给出较弱表述

这是典型的**上一回合短期记忆**。

#### 中期记忆

来源：`src/game/npcPromptContext.ts:63` 的 `describeRelationshipTemperature(...)`

- 扫描最近 2 回合 `roundHistory.slice(-2)`
- 统计你对该 NPC 是偏柔和手法还是偏强压手法
- 同时参考这两回合施计成功数

这是典型的**最近两回合中期关系记忆**。

#### 长期记忆

来源：`src/game/npcPromptContext.ts:96` 的 `describeRecentCourtFortune(...)`

长期层面不是全文叙事日志，而是把当前状态和初始基线做对比：

- court NPC：拿当前 faction 势能与 `INITIAL_FACTIONS` 比较
- external NPC：看 `externalStatus`、当前忠诚度相对 `INITIAL_NPCS` 的偏移
- 同时优先读取 `recentBacklash`

因此长期记忆本质上是：

- 初始基线
- 当前状态
- 最近余波

三者差分得出的**累计局势记忆**。

### 这些记忆在哪里被使用

- NPC 计谋回报页回复：
  - `src/components/SchemeFeedback/SchemeFeedback.tsx:297-301`
- NPC 回报 prompt：
  - `src/ai/prompts.ts:160`
  - `src/ai/prompts.ts:196-199`
  - `src/ai/prompts.ts:225-228`
- 冯道之代拟 prompt：
  - `src/ai/prompts.ts:614-617`

### 总结

当前北周 NPC 的跨回合记忆机制可以概括为：

- **短期**：最近 1 回合
- **中期**：最近 2 回合
- **长期**：当前状态相对初始状态的累计漂移 + 最近余波

这套方式的优点是稳定、可控、不会失忆到完全随机；缺点是它不是“真正叙事型长记忆”，更像结构化摘要系统。

相关测试覆盖：

- `src/game/npcPromptContext.test.ts`

---

## 5）检查北周朝堂角色死亡后是否还会在后续朝堂页/冯道之锦囊中显现，同时是否有提示外部军头不适用这条处决链路

### 结论

结论分两部分：

- **朝堂角色死亡后：朝堂页仍会显示，但会以死亡卡形式保留；冯道之朝局锦囊不会再继续把死者当作活跃对象强调。**
- **外部军头不适用“借刀处决链”这一点，在机制上成立，但目前没有发现明确的用户提示文案。**

### 朝堂角色死亡后的表现

借刀处决真正落地于 `src/game/roundSettlement.ts:295`：

- `relatedNpc.isAlive = false`
- `relatedNpc.militaryPower = 0`
- `deathCause = 'borrowed_blade'`

朝堂页对 court NPC 的列表是：

- `src/components/CourtView/CourtView.tsx:99`
  - `const courtNpcs = npcs.filter(npc => npc.powerBase === 'court')`

也就是说，**court NPC 即使死了，也不会从朝堂页数组里被删掉**。  
所以后续朝堂页仍会看到他，只是应以死亡/失效状态呈现。

冯道之朝局锦囊则会过滤死者：

- `src/game/roundIntelEngine.ts:20` 只建立 `isAlive` 的 lookup
- `src/game/roundIntelEngine.ts:38` 的 `getRoundAdvisorHint(...)` 依赖活人焦点
- `src/game/borrowedBladeEngine.ts:159-161` 的 `getHighestBorrowedBladeStageLabel(...)` 也只看 `npc.isAlive`

因此，**死亡 court NPC 不会继续成为冯道之锦囊里的活跃焦点**。

### 外部军头是否适用借刀处决链

机制上：**不适用**。

证据：

- `src/game/borrowedBladeEngine.ts:10` 的 `BORROWED_BLADE_TARGET_IDS` 只包含一组固定 court NPC
- `src/game/borrowedBladeEngine.ts:20` 的 `canUseBorrowedBladeDisposalStage(...)` 仅对这批 ID 返回 true
- `src/game/borrowedBladeEngine.test.ts:47-55` 也明确测试了：
  - `zuting` 可进入处决链
  - `hebaqi` 不可进入处决链

### 是否存在用户提示

本次核查中，**没有找到明确告诉玩家“外部军头不适用这条处决链路”的单独提示文案**。  
也就是说：

- 机制限制已经做了
- 测试也验证了部分目标不适用
- 但玩家侧说明仍不够直白

### 建议

后续最好在以下任一位置补一句明确说明：

- 借刀计谋指南
- 施计页借刀 hover 提示
- 外部军头玩法教学

建议文案方向：

“借刀可对外部军头施压，但‘猜忌链条 -> 正式处决’只适用于部分朝堂中枢人物，地方军头不会走这条链。”

---

## 6）外部军头触发叛乱/割据后是否还会在后续朝堂页/冯道之锦囊中出现

### 结论

结论：**分情况，而且这里存在一个值得修的边界问题。**

#### 情况 A：外部军头失败性叛乱，角色死亡

会消失于后续朝堂页，也不会继续在地方军头锦囊中出现。

证据：

- `src/game/roundSettlement.ts:679-680`
  - `targetNpc.isAlive = false`
  - `targetNpc.externalStatus = 'rebellion'`
- 朝堂页外部军头列表：
  - `src/components/CourtView/CourtView.tsx:100`
  - `const externalNpcs = npcs.filter(npc => npc.powerBase === 'external' && npc.isAlive)`

所以一旦外部军头在叛乱结算中死亡，他会从后续朝堂页直接消失。

首页地方军头锦囊的候选也先过滤 `npc.isAlive`：

- `src/game/externalActionHint.ts:45-47`

因此死亡的外部军头不会继续出现在首页地方军头锦囊。

#### 情况 B：外部军头成功割据

当前实现下，**仍会继续出现在朝堂页**。

原因：

- 成功割据时 `externalStatus = 'secession'`，但没有设为死亡
  - `src/game/roundSettlement.ts:632`
- 朝堂页只按 `isAlive` 过滤，不按 `externalStatus` 过滤
  - `src/components/CourtView/CourtView.tsx:100`

所以成功割据后的外部军头，仍会继续显示在后续朝堂页。

#### 情况 C：外部军头成功造反

当前实现中，“成功造反”最后也会落到 `externalStatus = 'secession'`：

- `src/game/roundSettlement.ts:664`

也就是说，它在系统里更像“先起兵，后坐实割据”，最终仍然是一个 **活着的 secession 状态 NPC**。  
因此它也会继续出现在后续朝堂页。

### 冯道之锦囊是否还会继续出现

这里要分两套逻辑：

#### 单体外部动作提示

`buildExternalActionStageHint(...)` 在 `src/game/externalActionHint.ts:31` 明确写了：

- 如果 `externalStatus === 'secession' || 'rebellion'`
- 直接 `return null`

这说明**单个军头提示逻辑本来是不想继续提示已经割据/叛乱的人**。

#### 首页“主导军头”锦囊

但首页实际用的是 `buildDominantExternalStageHint(...)`，见：

- `src/components/RoundStart/RoundStart.tsx:64`

这条函数在 `src/game/externalActionHint.ts:39` 中：

- 先按 `isAlive && powerBase === 'external'` 过滤
- 但**没有显式排除 `externalStatus === 'secession'` 或 `rebellion`**
- 它仍会继续对这些活着的外部军头调用 `buildExternalLineProgress(...)`

而 `buildExternalLineProgress(...)` 本身也没有一开始就排除 `secession/rebellion` 状态。  
因此这里存在一个边界问题：

> **成功割据后的外部军头，理论上仍有机会继续被首页“地方军头锦囊”选中并提醒。**

### 这一点应如何理解

所以第 6 点最准确的结论是：

- **失败叛乱且死亡**：不会再出现
- **成功割据 / 成功造反后转为割据状态**：会继续出现在朝堂页
- **首页地方军头锦囊**：存在继续点名已割据军头的风险，属于当前实现的边界漏洞/过滤不一致

### 建议

建议统一规则：

- 若外部军头进入 `secession` 或 `rebellion` 终态，就不再进入 `buildDominantExternalStageHint(...)` 的候选集合
- 朝堂页是否保留其卡片，可以作为“已明牌地方势力”保留展示，但状态应改成“已割据/已叛乱，不再可继续布局”

---

## 总结

### 本次确认已经成立的部分

- 冯道之地方军头锦囊是动态的，会随信任、忠诚、暗线、窗口变化。
- 谶纬代拟已经完整落地，前端、store、prompt、测试均已接通。
- 冯道之代拟 prompt 已经吃进大量动态状态，包括暗线、近两回合互动、派系压力、近期得失和玩家危险。
- 北周 NPC 的跨回合记忆机制已经成型，且属于结构化记忆方案。
- 借刀处决链机制上只适用于部分朝堂角色，外部军头不适用。

### 本次发现的边界问题

1. 冯道之代拟 prompt 还没有完整吃进蜀地/淮南战役结果分支，当前更像“施计页 UI 已动态，但军师 prompt 只部分动态”。
2. 外部军头进入成功割据后，首页地方军头锦囊与单体提示逻辑存在过滤不一致，可能继续被锦囊点名。
3. “外部军头不适用借刀处决链”目前缺少明确面向玩家的提示文案。

### 推荐优先级

- 高优先级：修正 `buildDominantExternalStageHint(...)` 对 `secession/rebellion` 的过滤不一致。
- 中优先级：把战役 resolvedState 接入 `buildFengDaozhiDraftContext(...)`。
- 中优先级：补一条面向玩家的“外部军头不走借刀处决链”说明。

---

## 补充核查：已转为割据/叛乱成功后割据状态的地方军头，是否彻底退出玩家可操作链路

### 目标状态

你这次补充确认的理想状态是：

- 已转为割据/叛乱成功后割据状态的地方军头，不再是玩家可选施计目标
- 不再在朝堂页显示公开表态
- 朝堂页角色卡显示明确终态，例如“已割据”
- 处理逻辑尽量接近“朝堂角色死亡后仍保留卡片，但进入终态展示”的模式

### 当前结论

结论：**这套终态退场逻辑目前没有完全落地。**

### 逐项核查结果

#### 1. 是否还会出现在施计页，作为玩家可选施计目标

结论：**会。当前未完全屏蔽。**

证据：

- `src/components/SchemePanel/SchemePanel.tsx:230`
  - 施计页目标列表直接取 `const aliveNpcs = npcs.filter(n => n.isAlive)`
- `src/components/SchemePanel/SchemePanel.tsx:578`
  - 目标选择按钮就是遍历 `aliveNpcs.map(...)`

而成功割据后，外部军头只是：

- `src/game/roundSettlement.ts:632`
  - `targetNpc.externalStatus = 'secession'`

并**没有**被置为死亡。  
因此只要他还活着，就仍会出现在施计页目标列表里。

需要注意的是：

- `src/game/schemeEngine.ts:93`
  - `canEscalateExternalAction` 只允许 `loyal/watchful` 继续走“煽动割据/煽动造反”
- 这意味着已割据军头虽然不能再继续走“割据/造反”类高阶动作，
- 但依然可能被玩家选中，并继续使用别的普通计谋。

这与“终态后彻底退出玩家可操作链路”的目标不一致。

#### 2. 是否还会在朝堂页显示公开表态

结论：**会。当前未屏蔽。**

证据：

- `src/components/CourtView/CourtView.tsx:100`
  - 朝堂页地方军头列表取 `const externalNpcs = npcs.filter(npc => npc.powerBase === 'external' && npc.isAlive)`
- `src/components/CourtView/CourtView.tsx:370`
  - 每张地方军头卡都会直接渲染 `getNpcRoundReaction(...)`

当前这里只按 `isAlive` 过滤，并没有按 `externalStatus === 'secession'` 或 `rebellion` 做额外终态处理。  
因此成功割据后仍会继续显示“本回合公开表态”。

#### 3. 是否已经在朝堂页角色卡上显示“已割据”状态

结论：**没有按你设想的终态方式落地，只是弱提示。**

当前地方军头卡片上有一个姿态标签：

- `src/components/CourtView/CourtView.tsx:21-25`
  - `externalStatus === 'secession'` 时返回 `割据将成`
  - `externalStatus === 'rebellion'` 时返回 `已露反迹`

也就是说，现在只是把它当作一条普通姿态文案显示在 stats 区里，而不是：

- 像朝堂角色死亡那样进入终态卡
- 明确显示“已割据”
- 禁用点击或改为残局信息展示

#### 4. 当前是否采用了“类似朝堂角色死亡”的处理逻辑

结论：**没有。**

朝堂角色死亡的处理方式是：

- `src/components/CourtView/CourtView.tsx:99`
  - court 角色不过滤掉
- `src/components/CourtView/CourtView.tsx:249`
  - 卡片会带 `dead` class
- `src/components/CourtView/CourtView.tsx:285`
  - 显示 `已死` overlay
- `disabled={!npc.isAlive}`

而地方军头当前的处理方式是：

- 只保留 `isAlive` 的人
- 没有 dead/secession/rebellion 专门终态卡
- 没有 overlay
- 没有“已割据”终态显示
- 没有屏蔽公开表态

所以这两套逻辑现在并不一致。

### 最终判断

如果以你的目标为准，这一条当前应判定为：

- **未落地**

更准确地说，是“只落了一半”：

- 数值/状态层面已经能把外部军头推到 `secession`
- 但展示层和交互层还没有把 `secession` 当作一个真正的终态来处理

### 如果后续要修，建议落地标准

建议统一为以下规则：

- 施计页：
  - `aliveNpcs` 改为只允许 `isAlive && externalStatus !== 'secession' && externalStatus !== 'rebellion'` 的外部军头进入候选
- 朝堂页：
  - 外部军头不要直接过滤掉 `secession`，而是保留卡片
  - 但卡片进入终态展示，不再显示公开表态
  - 终态角标/遮罩改成 `已割据`
- 若是失败叛乱并死亡：
  - 可继续沿用死亡逻辑或单独显示 `已伏诛`
- 冯道之地方军头锦囊：
  - `buildDominantExternalStageHint(...)` 不再把 `secession/rebellion` 终态角色纳入候选
